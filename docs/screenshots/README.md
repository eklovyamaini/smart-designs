# Screenshots

Place the following screenshots here to display them in the main README:

| Filename | How to capture |
|---|---|
| `step1-upload.png` | http://localhost:8001/ — Step 1 upload screen |
| `step3-results.png` | http://localhost:8001/ — Step 3 after a migration run |
| `metadata-configure.png` | http://localhost:8001/metadata — top of Configure Scope |
| `metadata-operations.png` | http://localhost:8001/metadata — Operations checkboxes panel |

All images are referenced in `../../README.md` as `docs/screenshots/<filename>`.
